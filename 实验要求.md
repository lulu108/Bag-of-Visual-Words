# 一、实验一整体定位

实验一不是单纯“跑一个 BoVW 代码”，而应该定位为：

> 以图像分类任务为背景，比较颜色直方图、HOG 特征、SIFT-BoVW 等传统视觉特征在分类效果、运行时间和表达能力上的差异，分析人工特征方法在深度学习出现前的图像表示思想。

这样写会更有深度。

可以在报告里这样引入：

```
传统图像分类方法通常遵循“特征提取—特征编码—分类器训练”的流程。与深度学习端到端学习特征不同，传统方法需要人为设计图像特征，例如颜色直方图、HOG 梯度方向直方图和 SIFT 局部描述子。视觉词袋模型进一步借鉴文本词袋思想，将一张图像中不定数量的局部特征编码为固定长度的视觉词频直方图，从而使传统分类器能够处理图像数据。
```

---

# 二、实验一的核心流程

## 1. 数据集准备

你需要准备一个多类别图像分类数据集。

建议数据集格式整理成：

```
dataset/├── class_1/│   ├── 001.jpg│   ├── 002.jpg│   └── ...├── class_2/│   ├── 001.jpg│   ├── 002.jpg│   └── ...├── class_3/└── ...
```

如果你的 GitHub 代码支持这种目录结构，就直接按这个来。

推荐使用：

```
Caltech-101 子集
```

建议先选 5 类到 10 类，例如：

```
airplanesFacesMotorbikescar_sidewatchbonsaicamerachair
```

如果你想控制难度，建议：

```
5 类 × 每类 80 张左右
```

如果你想报告更丰富，可以：

```
8 类 × 每类 80～100 张左右
```

数据集划分建议：

| 数据集 | 比例 |
| ------ | ---- |
| 训练集 | 70%  |
| 测试集 | 30%  |

或者：

| 数据集 | 比例 |
| ------ | ---- |
| 训练集 | 60%  |
| 验证集 | 20%  |
| 测试集 | 20%  |

实验报告中可以写：

```
为保证实验可复现性，本文按照类别进行分层划分，使每一类图像在训练集和测试集中均保持相近比例，避免由于类别分布不均导致评价结果偏差。
```

---

## 2. 图像预处理

预处理一般包括：

```
读取图像↓统一尺寸↓灰度化或颜色空间转换↓特征提取
```

不同特征需要不同预处理：

| 特征方法   | 输入要求       |
| ---------- | -------------- |
| 颜色直方图 | RGB / HSV 图像 |
| HOG        | 灰度图像       |
| SIFT-BoVW  | 灰度图像       |
| ORB-BoVW   | 灰度图像       |

建议统一 resize 到：

```
224 × 224
```

或者：

```
256 × 256
```

报告里可以写：

```
由于原始图像尺寸不一致，为保证特征维度和运行时间具有可比性，实验首先将所有图像统一缩放至固定尺寸。对于 HOG、SIFT 等基于灰度或梯度信息的特征，进一步将图像转换为灰度图；对于颜色直方图，则保留颜色通道并在 HSV 空间中统计颜色分布。
```

---

# 三、主方法：SIFT-BoVW 图像分类流程

这是你实验的核心。

## 1. SIFT 特征提取

SIFT 的作用是从每张图像中提取局部关键点和描述子。

一张图像可能提取出：

```
100 个关键点300 个关键点500 个关键点
```

每个关键点对应一个：

```
128 维 SIFT 描述子
```

所以一张图像不是一个固定长度向量，而是：

```
N × 128
```

其中 N 是该图像检测到的关键点数量。

报告中可以这样解释：

```
SIFT 特征具有一定的尺度和旋转不变性，能够较稳定地描述图像中的局部纹理、边缘和角点结构。但由于不同图像中关键点数量不同，SIFT 描述子本身不能直接输入分类器。因此需要借助视觉词袋模型将不定数量的局部特征编码为固定长度的图像级特征。
```

---

## 2. 构建视觉词典

把训练集中所有图像的 SIFT 描述子收集起来：

```
图像1：N1 × 128图像2：N2 × 128图像3：N3 × 128...
```

合并成：

```
M × 128
```

然后使用 KMeans 聚类：

```
KMeans 聚类中心 = 视觉词
```

如果设置：

```
K = 100
```

表示构建 100 个视觉词。

每个聚类中心代表一种常见的局部视觉模式。

流程如下：

```
训练集 SIFT 描述子↓KMeans 聚类↓K 个聚类中心↓视觉词典 Codebook
```

报告中可以写：

```
视觉词典的构建是 BoVW 模型的关键步骤。本文将训练集中提取到的 SIFT 描述子作为聚类样本，采用 KMeans 将局部描述子划分为 K 个簇，每个簇中心被视为一个视觉词。这样，原本连续的局部特征空间被离散化为有限个视觉词，后续每张图像都可以通过统计视觉词出现频次来表示。
```

---

## 3. BoVW 直方图编码

对一张图像的每个 SIFT 描述子，找到最近的视觉词：

```
descriptor_i → nearest visual word_j
```

然后统计每个视觉词出现次数。

如果 K=100，那么每张图像变成：

```
100 维直方图
```

例如：

```
[0, 2, 5, 0, 1, 8, ..., 3]
```

这就是图像的 BoVW 表示。

由于不同图像关键点数量不同，所以需要归一化：

```
L1 归一化 / L2 归一化
```

报告中可以写：

```
BoVW 编码将一张图像中所有局部描述子映射到视觉词典中，并统计各视觉词的出现频率。该过程把不定数量的局部特征转换为固定维度的直方图表示，使得 SVM、KNN 等传统分类器能够直接处理图像样本。由于不同图像提取到的关键点数量不同，本文对词频直方图进行归一化处理，以减弱关键点数量差异对分类结果的影响。
```

---

## 4. 分类器训练

BoVW 直方图生成后，每张图像就是一个固定长度向量：

```
X_train: num_images × Ky_train: num_images
```

然后训练分类器。

建议主分类器使用：

```
SVM
```

可以对比：

```
Linear SVMRBF SVMKNNRandom Forest
```

但报告不要太散，主推：

```
SIFT-BoVW + SVM
```

可以附加一个 KNN 作为对比。

分类器部分可以写：

```
在完成 BoVW 特征编码后，本文采用支持向量机作为分类器。SVM 适合处理中小规模、高维稀疏特征，在传统图像分类任务中常与 BoVW 特征结合使用。实验中分别比较线性核 SVM 与 RBF 核 SVM 的性能差异，以分析分类器复杂度对分类效果和运行时间的影响。
```

---

# 四、传统特征对比设计

你的题目里有“传统特征对比分析”，所以一定不能只写 BoVW。建议设置以下对比方法：

## 方法一：颜色直方图 + SVM

颜色直方图是最简单 baseline。

流程：

```
RGB/HSV 图像
↓
统计各通道颜色直方图
↓
拼接成固定长度向量
↓
SVM 分类
```

特点：

| 项目   | 说明                     |
| ------ | ------------------------ |
| 优点   | 提取快，计算简单         |
| 缺点   | 不关注形状和局部结构     |
| 适合   | 颜色差异明显的类别       |
| 不适合 | 颜色相似但形状不同的类别 |

报告分析可以写：

```
颜色直方图能够描述图像整体颜色分布，在类别颜色差异明显时具有一定分类能力。但它忽略了目标形状、纹理和空间布局，因此当不同类别具有相似颜色分布时容易发生误判。
```

---

## 方法二：HOG + SVM

HOG 是经典形状特征。

流程：

```
灰度图像↓计算局部梯度方向↓统计方向直方图↓形成 HOG 特征向量↓SVM 分类
```

特点：

| 项目   | 说明                       |
| ------ | -------------------------- |
| 优点   | 能表达边缘和轮廓结构       |
| 缺点   | 对尺度、姿态变化较敏感     |
| 适合   | 轮廓明显的目标             |
| 不适合 | 纹理复杂或姿态变化大的目标 |

报告分析可以写：

```
HOG 特征通过统计局部区域内的梯度方向分布描述图像形状结构，相比颜色直方图能够更好地反映目标轮廓和边缘信息。但 HOG 依赖固定窗口和局部网格划分，当目标尺度、方向或姿态变化较大时，其特征稳定性会下降。
```

---

## 方法三：SIFT-BoVW + SVM

这是主方法。

特点：

| 项目   | 说明                                 |
| ------ | ------------------------------------ |
| 优点   | 局部特征鲁棒，适合纹理和局部结构表达 |
| 缺点   | 计算较慢，丢失空间位置信息           |
| 适合   | 局部结构明显、纹理丰富的类别         |
| 不适合 | 强依赖整体布局和语义关系的类别       |

报告分析可以写：

```
SIFT-BoVW 方法能够利用局部关键点描述图像中的纹理、角点和局部结构，并通过视觉词典将局部特征编码为图像级表示。与颜色直方图和 HOG 相比，BoVW 对局部变化具有更强鲁棒性，但其计算过程包括 SIFT 提取、KMeans 聚类和视觉词映射，整体时间开销更高。同时，BoVW 只统计视觉词出现频率，不保留视觉词之间的空间位置关系，因此在需要全局结构判断的类别上仍存在局限。
```

---

## 可选方法四：ORB-BoVW + SVM

如果你的代码里有 ORB，建议加上。

ORB 是 SIFT 的轻量替代。

| 方法      | 描述子           | 速度 | 效果 |
| --------- | ---------------- | ---- | ---- |
| SIFT      | 128 维浮点描述子 | 慢   | 稳定 |
| ORB       | 二进制描述子     | 快   | 略弱 |
| SIFT-BoVW | KMeans + SVM     | 中慢 | 较好 |
| ORB-BoVW  | KMeans + SVM     | 较快 | 中等 |

可以写：

```
为了进一步分析局部特征类型对 BoVW 分类结果的影响，实验加入 ORB-BoVW 作为对比。ORB 特征提取速度更快，适合实时场景，但其描述能力通常弱于 SIFT，因此在分类准确率上可能略低。
```

---

# 五、建议的实验对比表设计

## 1. 不同特征方法总体对比

你最终报告里一定要有这张表：

| 方法            | 特征维度 | 分类器 | Accuracy | Macro-F1 | 特征提取时间 | 训练时间 | 测试时间 |
| --------------- | -------- | ------ | -------- | -------- | ------------ | -------- | -------- |
| Color Histogram | 待填     | SVM    | 待填     | 待填     | 待填         | 待填     | 待填     |
| HOG             | 待填     | SVM    | 待填     | 待填     | 待填         | 待填     | 待填     |
| SIFT-BoVW       | K        | SVM    | 待填     | 待填     | 待填         | 待填     | 待填     |
| ORB-BoVW        | K        | SVM    | 待填     | 待填     | 待填         | 待填     | 待填     |

分析方向：

```
颜色直方图速度最快，但表达能力最弱；HOG 能表达形状结构，效果通常优于颜色直方图；SIFT-BoVW 结合局部特征与聚类编码，分类能力更强，但计算时间更长；ORB-BoVW 速度较快，但描述能力可能不如 SIFT-BoVW。
```

---

## 2. 不同视觉词典大小 K 的影响

BoVW 必须分析 K 值。

建议设置：

```
K = 50, 100, 200, 400
```

表格：

| 视觉词典大小 K | Accuracy | Macro-F1 | KMeans 训练时间 | 编码时间 | SVM 训练时间 |
| -------------- | -------- | -------- | --------------- | -------- | ------------ |
| 50             | 待填     | 待填     | 待填            | 待填     | 待填         |
| 100            | 待填     | 待填     | 待填            | 待填     | 待填         |
| 200            | 待填     | 待填     | 待填            | 待填     | 待填         |
| 400            | 待填     | 待填     | 待填            | 待填     | 待填         |

分析可以这样写：

```
随着视觉词典规模 K 的增大，BoVW 特征能够更细致地区分不同局部模式，分类准确率通常会有所提升。但当 K 过大时，特征维度增加，KMeans 聚类和图像编码时间明显上升，同时可能由于训练样本不足导致视觉词分布稀疏，分类性能提升不明显甚至下降。因此，K 的选择需要在表达能力和计算复杂度之间进行权衡。
```

---

## 3. 不同分类器对比

如果你想更完整，可以加：

| 特征      | 分类器        | Accuracy | Macro-F1 | 训练时间 |
| --------- | ------------- | -------- | -------- | -------- |
| SIFT-BoVW | KNN           | 待填     | 待填     | 待填     |
| SIFT-BoVW | Linear SVM    | 待填     | 待填     | 待填     |
| SIFT-BoVW | RBF SVM       | 待填     | 待填     | 待填     |
| SIFT-BoVW | Random Forest | 待填     | 待填     | 待填     |

分析：

```
KNN 不需要显式训练，但测试阶段需要计算样本间距离，预测速度较慢；Linear SVM 训练速度较快，适合高维直方图特征；RBF SVM 能处理非线性边界，但训练时间较长，对参数 C 和 gamma 更敏感；Random Forest 对特征尺度不敏感，但在高维稀疏直方图上不一定优于 SVM。
```

---

# 六、实验流程可以这样写进报告

你可以直接使用下面这段作为报告“实验方法”部分。

```
本实验首先对图像数据集进行整理和划分，将各类别图像按照一定比例划分为训练集和测试集。随后对输入图像进行统一尺寸缩放，以保证不同特征提取方法在相同输入条件下进行比较。在主方法 SIFT-BoVW 中，首先使用 SIFT 算法从训练图像中提取局部关键点描述子，每个描述子为 128 维向量。由于每张图像的关键点数量不同，无法直接作为分类器输入，因此实验进一步收集训练集中所有图像的 SIFT 描述子，并采用 KMeans 聚类构建视觉词典。聚类中心被视为视觉词，词典大小 K 表示视觉词数量。在完成视觉词典构建后，将每张图像中的 SIFT 描述子映射到最近的视觉词，并统计各视觉词的出现频率，形成 K 维 BoVW 直方图。为减小不同图像关键点数量差异带来的影响，对直方图进行归一化处理。最终，将 BoVW 特征输入 SVM 分类器进行训练，并在测试集上计算 Accuracy、Macro-F1、混淆矩阵和运行时间等指标。为了全面分析传统图像特征的差异，实验同时设置颜色直方图和 HOG 特征作为对比方法。颜色直方图主要描述图像整体颜色分布，HOG 特征主要描述局部梯度和轮廓结构，而 SIFT-BoVW 则通过局部关键点和视觉词典描述图像中的局部结构模式。通过对三类特征在相同分类器和相同数据集上的表现进行比较，可以更清晰地分析不同传统视觉特征在图像分类任务中的适用场景与局限性。
```

---

# 七、你这份实验报告重点应该分析什么？

你要拿高分，不能只写“准确率多少”。重点要写下面几个分析。

## 1. 特征表达能力分析

可以这样写：

```
颜色直方图主要关注颜色分布，计算速度快，但忽略目标形状和空间结构，因此在颜色差异明显的类别上表现较好，在颜色相似类别上容易混淆。HOG 特征通过统计梯度方向描述图像局部轮廓，比颜色直方图更能反映目标形状信息，因此在车辆、飞机、人脸等具有明显边缘结构的类别上通常表现更好。但 HOG 对尺度变化、旋转变化和复杂背景较敏感。SIFT-BoVW 利用局部关键点描述图像中的稳定局部结构，并通过视觉词典将局部描述子编码为图像级直方图。该方法相比颜色直方图和 HOG 具有更强的局部鲁棒性，但由于忽略了视觉词的空间分布，仍然难以表达全局结构和高级语义关系。
```

---

## 2. 运行时间分析

可以这样写：

```
从运行时间看，颜色直方图的特征提取速度最快，因为它只需要统计像素颜色分布；HOG 需要计算梯度方向和局部块归一化，耗时略高；SIFT-BoVW 的计算开销最大，主要来自三个部分：SIFT 关键点检测与描述子计算、KMeans 视觉词典训练以及描述子到视觉词的匹配编码。因此，SIFT-BoVW 在分类性能上可能更优，但需要付出更高的时间成本。
```

---

## 3. K 值影响分析

可以这样写：

```
视觉词典大小 K 是影响 BoVW 性能的重要参数。当 K 较小时，多个不同局部结构可能被映射到同一个视觉词，导致特征区分能力不足；随着 K 增大，视觉词典能够表示更细粒度的局部模式，分类性能可能提升。但当 K 过大时，BoVW 直方图维度增大，特征分布变得稀疏，KMeans 聚类和编码时间明显增加，同时可能出现过拟合。因此，实验中需要结合 Accuracy、Macro-F1 和运行时间综合选择合适的 K 值。
```

---

## 4. 混淆矩阵分析

这个很重要，能体现你不是只会跑代码。

可以这样写：

```
从混淆矩阵可以观察到，不同类别之间的误分类往往与视觉外观相似性有关。例如，当两个类别具有相似的颜色背景或局部纹理时，颜色直方图和 BoVW 方法容易出现混淆；当目标轮廓差异明显时，HOG 特征通常能够取得更好的区分效果。SIFT-BoVW 虽然能够捕捉局部纹理结构，但由于缺乏空间布局信息，当不同类别共享相似局部结构时仍可能发生误判。
```

---

## 5. BoVW 局限性分析

可以这样写：

```
BoVW 模型的主要局限在于忽略空间结构。它只统计视觉词出现频率，不关心视觉词出现在图像的哪个位置，因此两张局部视觉词分布相似但空间布局不同的图像可能得到相似表示。此外，BoVW 的性能依赖 SIFT 特征质量和视觉词典构建效果，当图像纹理较少、光照变化明显或背景干扰较强时，分类性能可能下降。相比深度学习方法，BoVW 不能端到端优化特征提取过程，语义表达能力有限。
```

---

# 八、实验一建议的章节结构

你的报告第一部分可以这样写：

```
1 实验目的1.1 理解传统图像分类流程1.2 掌握 SIFT-BoVW 特征编码方法1.3 比较不同传统特征的分类性能2 实验原理2.1 图像分类基本流程2.2 颜色直方图特征2.3 HOG 特征2.4 SIFT 局部特征2.5 视觉词袋模型 BoVW2.6 SVM 分类器3 实验设计3.1 数据集选择与划分3.2 图像预处理3.3 SIFT-BoVW 主方法流程3.4 对比方法设置3.5 评价指标4 实验结果4.1 SIFT 关键点可视化4.2 BoVW 直方图可视化4.3 不同特征方法分类结果对比4.4 不同 K 值对 BoVW 的影响4.5 混淆矩阵分析4.6 运行时间对比5 实验分析5.1 不同特征表达能力分析5.2 视觉词典规模影响分析5.3 分类错误样例分析5.4 方法优缺点与适用场景6 实验总结
```

---

# 九、建议你最终做出的图和表

至少准备这些：

| 类型 | 内容                                |
| ---- | ----------------------------------- |
| 图 1 | 实验整体流程图                      |
| 图 2 | 数据集样例图                        |
| 图 3 | SIFT 关键点可视化                   |
| 图 4 | BoVW 视觉词频直方图                 |
| 图 5 | 不同方法 Accuracy / Macro-F1 柱状图 |
| 图 6 | 不同 K 值下 Accuracy 曲线           |
| 图 7 | 混淆矩阵                            |
| 表 1 | 数据集类别与样本数                  |
| 表 2 | 不同特征方法结果对比                |
| 表 3 | 不同 K 值结果对比                   |
| 表 4 | 运行时间对比                        |
| 表 5 | 不同方法优缺点总结                  |

---

# 十、如果你的代码仓库是标准 BoVW 代码，通常会对应这些模块

你可以检查一下 `Bag-of-Visual-Words.git` 里是否有类似文件：

```
extract_features.pytrain_codebook.pybuild_histogram.pytrain_classifier.pypredict.pymain.pyutils.py
```

一般对应关系是：

| 代码模块            | 实验流程                     |
| ------------------- | ---------------------------- |
| 数据读取模块        | 读取不同类别图像和标签       |
| SIFT 提取模块       | 从每张图像提取局部描述子     |
| KMeans 模块         | 训练视觉词典                 |
| Histogram/BoVW 模块 | 生成每张图像的视觉词直方图   |
| Classifier 模块     | 训练 SVM/KNN 分类器          |
| Evaluation 模块     | 输出准确率、混淆矩阵         |
| Visualization 模块  | 展示关键点、直方图、分类结果 |

你后面写报告时可以把代码实现和实验流程对应起来。

---

# 十一、实验一最终结论可以提前这样设计

等你跑完后，可以按这个方向写结论：

```
实验结果表明，传统图像分类方法的性能高度依赖特征表示方式。颜色直方图计算速度最快，但由于缺乏形状和纹理信息，分类能力有限；HOG 特征能够表达目标边缘和轮廓结构，在具有明显形状差异的类别上表现较好；SIFT-BoVW 通过局部关键点和视觉词典构建图像级表示，通常能够获得更好的分类效果，但其特征提取和视觉词典训练耗时较高。此外，视觉词典大小 K 对 BoVW 性能具有明显影响。较小的 K 值导致视觉词表达过于粗糙，较大的 K 值虽然提高了局部模式表达能力，但也增加了特征维度和计算开销。综合准确率、Macro-F1 和运行时间后，选择适中的 K 值更有利于在分类效果和效率之间取得平衡。总体来看，BoVW 方法较好地体现了传统计算机视觉“局部特征提取—特征编码—分类器训练”的技术路线，但其忽略空间结构和语义表达能力不足的问题也较为明显，这为后续深度学习图像分割和 YOLO 目标检测实验提供了技术对比基础。
```

---

# 十二、你现在应该怎么做
